# admin-dashboard-2
I know it has some issues (particularly with unncessary use of the ::before pseudo-element)
Could fix that, but I would have to go back and reformat everything which I feel is just not worth it for this - I already did if for other things that were much more important.

So I'll just leave a reflection on it instead - 
For the headings above cards, you should just add more rows into the grid instead of using  
a pseudo-element, it's tricky and will fail when you shrink the page.

Same goes for the 'dashboard' logo. Should have just put the image beside the dashboard text, because when the page is smaller, it touches the logo. (it is not recognised.?).